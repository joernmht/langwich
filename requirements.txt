# Runtime dependency (worksheet generation + PDF rendering).
# This is the single dependency; see pyproject.toml for the authoritative list
# and the optional [dev] extras (pytest, ruff, mypy).
reportlab>=4.1
